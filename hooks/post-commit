#!/bin/sh
if [ -f .git/hooks/hooksrc ]
then
    . .git/hooks/hooksrc
    for enabled_plugin in $php_plugins
    do
        if [ -f .git/hooks/php/$enabled_plugin/post-commit ]
        then
            . .git/hooks/php/$enabled_plugin/post-commit
        fi
    done
fi
